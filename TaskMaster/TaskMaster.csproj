<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <BuildNumber Condition=" '$(APPVEYOR_BUILD_NUMBER)' != '' ">$(APPVEYOR_BUILD_NUMBER)</BuildNumber>
    <!-- for AppVeyor -->
    <BuildNumber Condition=" '$(BUILD_NUMBER)' != '' ">$(BUILD_NUMBER)</BuildNumber>
    <!-- for Team City -->
    <BuildNumber Condition=" '$(BuildNumber)' == '' ">0</BuildNumber>
    <!-- if not set -->
    <Description>TaskMaster is a simple library used to manage sets of fire and forget tasks that need to run after specific dates/times.</Description>
    <AssemblyTitle>TaskMaster</AssemblyTitle>
    <Authors>James Craig</Authors>
    <TargetFramework>netstandard2.0</TargetFramework>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <AssemblyName>TaskMaster</AssemblyName>
    <PackageId>TaskMaster</PackageId>
    <PackageTags>Tasks;Jobs;Cron</PackageTags>
    <PackageProjectUrl>https://github.com/JaCraig/TaskMaster</PackageProjectUrl>
    <PackageLicenseUrl>https://github.com/JaCraig/TaskMaster/blob/master/LICENSE</PackageLicenseUrl>
    <GenerateAssemblyConfigurationAttribute>false</GenerateAssemblyConfigurationAttribute>
    <GenerateAssemblyCompanyAttribute>false</GenerateAssemblyCompanyAttribute>
    <GenerateAssemblyProductAttribute>false</GenerateAssemblyProductAttribute>
    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
    <Version>2.0.$(BuildNumber)</Version>
    
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="BigBook" Version="2.0.0" />
    <PackageReference Include="Canister.IoC" Version="2.0.0" />
    <PackageReference Include="FileCurator" Version="2.0.0" />
    <PackageReference Include="SerialBox" Version="2.0.0" />
    <PackageReference Include="Serilog" Version="2.6.0" />
  </ItemGroup>

</Project>